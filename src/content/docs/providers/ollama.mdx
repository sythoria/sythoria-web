---
title: Ollama (Local)
description: Run AI models locally with Ollama — no API key, no cloud, no tracking.
---

# Ollama (Local)

Use Sythoria with models running locally through Ollama. Run state-of-the-art open-weights models including Llama 4 (Scout and Maverick), DeepSeek R1, and Gemma 4. No API key, no cloud dependency, no data leaving your machine — just fast, private AI on your own hardware.

## Prerequisites

- [Ollama](https://ollama.com) installed on your machine
- At least one model pulled locally

## Setup

1. Install Ollama from [ollama.com](https://ollama.com) (or via Homebrew: `brew install ollama`)
2. Pull a model:

   ```bash
   ollama pull llama4:maverick
   ```

3. Start the Ollama server:

   ```bash
   ollama serve
   ```

4. In Sythoria, open **Settings** from the sidebar
5. Select **Ollama (Local)** from the **Provider Preset** dropdown — the API base URL and default model fill in automatically
6. Leave the **API Key** field empty (Ollama doesn't require one)
7. The connection indicator should turn **green** once Ollama is detected

> Ollama must be running while you chat. If you close the Ollama server, Sythoria will show a red connection indicator.

## Default configuration

| Setting           | Value                                        |
| ----------------- | -------------------------------------------- |
| **API Base**      | `http://localhost:11434/v1/chat/completions` |
| **Default Model** | `llama4-maverick`                            |
| **API Format**    | OpenAI-compatible                            |
| **Streaming**     | Supported                                    |
| **API Key**       | None required                                |

## Popular models

| Model            | Size                    | Pull command                  | Best for                                                    |
| ---------------- | ----------------------- | ----------------------------- | ----------------------------------------------------------- |
| Llama 4 Scout    | 17B active (109B total) | `ollama pull llama4:scout`    | Efficient, high-speed Mixture-of-Experts (MoE) reasoning    |
| Llama 4 Maverick | 17B active (400B total) | `ollama pull llama4:maverick` | Complex reasoning, advanced coding, and multi-GPU setups    |
| DeepSeek R1      | 8B / 14B / 32B / 70B    | `ollama pull deepseek-r1`     | Advanced reasoning, step-by-step logic, and math            |
| Gemma 4          | 12B / 26B / 31B         | `ollama pull gemma4`          | Google's open multimodal model (text, image, audio support) |

Browse all available models at [ollama.com/library](https://ollama.com/library).

## Model selection

Ollama models are entered manually in Settings under **Model ID** (e.g., `llama4-maverick`, `llama4:scout`, `deepseek-r1`, or `gemma4`). Ensure you have downloaded the model locally via the Ollama command line before attempting to use it in Sythoria.

## Hardware recommendations

Local model performance depends entirely on your hardware. As a rough guide:

| Model size | Minimum RAM | Recommended RAM | GPU                           |
| ---------- | ----------- | --------------- | ----------------------------- |
| 3B–7B      | 8 GB        | 16 GB           | Optional (CPU works)          |
| 13B–14B    | 16 GB       | 32 GB           | Recommended                   |
| 32B–34B    | 32 GB       | 64 GB           | Strongly recommended          |
| 70B+       | 64 GB       | 128 GB          | Required for acceptable speed |

> **Tip:** Use quantized models (Q4_K_M) for better performance on consumer hardware. Ollama downloads quantized versions by default.

## Running Ollama on a different port

If you've configured Ollama to listen on a non-default port, update the **API Base URL** in Sythoria's settings:

```
http://localhost:<your-port>/v1/chat/completions
```

## Troubleshooting

| Error                           | Cause                                   | Fix                                                                                                 |
| ------------------------------- | --------------------------------------- | --------------------------------------------------------------------------------------------------- |
| **Connection refused**          | Ollama isn't running                    | Run `ollama serve` in a terminal, or start the Ollama app                                           |
| **Model not found**             | Model hasn't been pulled yet            | Run `ollama pull <model-name>` to download it                                                       |
| **Slow responses**              | Hardware too limited for the model size | Try a smaller model or a quantized variant                                                          |
| **Out of memory**               | Model exceeds available RAM/VRAM        | Switch to a smaller model (e.g., 7B instead of 70B)                                                 |
| **Connection indicator red**    | Can't reach `localhost:11434`           | Ensure Ollama is running and no firewall is blocking port 11434                                     |
| **Access Denied / Local Block** | Backend security blocks local networks  | Open `config.json` in your App Data folder, and add `"allowLocalNetwork": true` to the model block. |
